# Docker Port Mapping

# Coordinator external port (mapped to container port from coordinator/.env)
COORDINATOR_EXTERNAL_PORT=8080

# PostgreSQL Configuration
POSTGRES_EXTERNAL_PORT=5432
POSTGRES_PASSWORD=your_secure_postgres_password_here

# Redis Configuration
REDIS_EXTERNAL_PORT=6379
REDIS_PASSWORD=your_secure_redis_password_here

# IMPORTANT:
# 1. Copy this file to .env and generate strong passwords
# 2. Never commit .env to git (it's in .gitignore)
# 3. Use strong random passwords (32+ characters)
#
# Generate passwords with:
#   openssl rand -base64 32
#   or
#   LC_ALL=C tr -dc 'A-Za-z0-9' < /dev/urandom | head -c 32
