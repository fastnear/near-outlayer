# Keystore Worker Configuration

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8081

# Master Secret for Key Derivation (HMAC-SHA256)
# Generate: openssl rand -hex 32
# IMPORTANT: Keep this secret safe! All repo-based secrets depend on it.
KEYSTORE_MASTER_SECRET=your_master_secret_hex_64_chars

# NEAR Configuration for Reading Secrets from Contract
# Both are REQUIRED for repo-based secrets to work:
NEAR_RPC_URL=https://rpc.testnet.near.org
NEAR_CONTRACT_ID=offchainvm.testnet

# Security
# SHA256 hash of bearer tokens that executor workers must provide
# Example: echo -n "my-secure-token-123" | sha256sum
ALLOWED_WORKER_TOKEN_HASHES=cbd8f6f0e3e8ec29d3d1f58a2c8c6d6e8d7f5a4b3c2d1e0f1a2b3c4d5e6f7a8b

# TEE Configuration (for future use)
# TEE_MODE=sgx|sev|simulated|none
TEE_MODE=none

# Logging
RUST_LOG=info,keystore_worker=debug
